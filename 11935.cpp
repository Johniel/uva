#include <iostream>
#include <algorithm>
#include <sstream>

using namespace std;

int main(void)
{
  while( !cin.eof() ){
    string s, t, u;
    int a, b, c, d, n, m;
    cin >> a >> s >> s >> n;
    int m = 0;
    int mx = -1;
    while(true){
      cin >> d >> s;
      if()
    }
    
  }
  return 0;
}
