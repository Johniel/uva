#include <iostream>
#include <algorithm>
#include <cmath>
#include <cstdio>

using namespace std;

int main(void)
{
  double r;
  while( cin >> r ){
    double a, b, c;
    a = r * r * ();
    b = r * r * ();
    c = r * r * ();
    printf("%0.0lf %0.3lf %0.3lf\n", a, b, c);
  }
  return 0;
}
   s1=r*r*(1.0-pi/6.0-sqrt(3.0)/4.0);
   s2=r*r*(pi/12.0+sqrt(3.0)/2.0-1.0);
   s3=r*r*(1.0+pi/3.0-sqrt(3.0));
