#include <iostream>
#include <algorithm>

using namespace std;

int main(void)
{
  int num[4];
  for(;;){
    cin >> num[0] >> num[1] >> num[2] >> num[3];
    if(num[0] + num[1] + num[2] + num[3] == 0)break;
    
  }
  return 0;
}
